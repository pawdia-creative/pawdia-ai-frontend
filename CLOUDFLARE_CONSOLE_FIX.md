# Cloudflare 控制台手动修复指南

## 问题诊断

尽管我们已经在代码中配置了正确的 Pages 部署设置，但 Cloudflare 仍然在执行 `npx wrangler deploy` 命令。这表明在 Cloudflare 控制台中的项目设置需要手动更新。

## 根本原因

Cloudflare 控制台中的 "Deploy command" 设置覆盖了项目文件中的配置，仍然指向 Workers 部署命令。

## 解决方案步骤

### 1. 登录 Cloudflare 控制台
- 访问 [https://dash.cloudflare.com](https://dash.cloudflare.com)
- 登录你的账户

### 2. 找到你的项目
- 在左侧菜单中选择 "Workers & Pages"
- 找到你的项目（可能是 pawdia-ai 或类似名称）

### 3. 检查项目类型
**重要**：确认你看到的是 "Pages" 项目而不是 "Workers" 项目：
- Pages 项目图标是一个网页图标 📄
- Workers 项目图标是一个齿轮图标 ⚙️

### 4. 更新构建设置
1. 点击你的项目进入详情页
2. 点击 "Settings" 标签
3. 找到 "Builds" 或 "Build configurations" 部分
4. 更新以下设置：

#### 构建配置：
- **Framework preset**: Vite
- **Build command**: `npm run build`
- **Build output directory**: `frontend-separation/dist`
- **Install command**: `npm install`

#### 部署命令（关键）：
- **Deploy command**: **留空**（不要输入任何内容）

### 5. 环境变量设置
在 "Environment variables" 部分添加：
```
NODE_VERSION=18
VITE_API_URL=https://your-api-domain.com
VITE_AI_API_KEY=your-ai-api-key
```

### 6. 重新部署
1. 保存所有设置
2. 点击 "Deploy" 或 "Trigger deploy" 按钮
3. 等待部署完成

## 验证部署成功

成功部署后，你应该看到：
- ✅ 构建成功（没有 wrangler 错误）
- ✅ Pages 部署成功
- ✅ 静态文件正确部署

## 如果仍然失败

### 选项 1：完全删除并重新创建项目
1. 删除现有的 Workers/Pages 项目
2. 创建新的 **Pages** 项目（确保选择 Pages 而不是 Workers）
3. 连接相同的 GitHub 仓库
4. 按照上面的设置进行配置

### 选项 2：联系 Cloudflare 支持
如果问题持续存在，可能需要 Cloudflare 支持团队手动将你的项目从 Workers 转换为 Pages。

## 项目文件说明

我们已经创建了以下配置文件：

- `wrangler.jsonc` - 明确指定静态资产部署
- `.pages.toml` - Pages 配置文件
- `pages.json` - 备用 Pages 配置
- `package.json` - 包含正确的构建脚本

## 重要提醒

⚠️ **不要在 "Deploy command" 中使用 `npx wrangler deploy`**
⚠️ **确保选择 Pages 项目类型，不是 Workers**
⚠️ **部署命令字段必须留空**